#!/usr/bin/env bash
# 启动 WebArena 可视化服务(FastAPI 仪表盘)。
# 用法: bin/wa_view [--port 8100] [--host 0.0.0.0] [--reload]
#   浏览器开 http://localhost:<port>/
set -e
cd "$(dirname "$0")/.."
exec uv run python benchmark/webarena/server.py "$@"
