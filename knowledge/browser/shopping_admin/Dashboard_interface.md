---
id: knowledge.browser.shopping_admin.dashboard_interface
source_type: knowledge_interface
platform: browser
app: shopping_admin
scope:
  - orchestrator
selector_when: top search terms most used store dashboard usage results
source: manual_curated
confidence: high
ttl: session
---
# Dashboard interface

**Top Search Terms** is a read-only collection with **Search Term** (`text`),
**Results** (`number`), and **Uses** (`number`). Uses is the usage count for ranking most-used
terms; Search Term is the returned term text.
